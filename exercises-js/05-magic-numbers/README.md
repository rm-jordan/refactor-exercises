# JS 05 — Magic numbers

Same as TypeScript exercise 05, in plain JavaScript.

**Focus:** Name `18` and `65` as constants.

```bash
npx vitest run exercises-js/05-magic-numbers
npm run reset:js -- 05-magic-numbers
```
