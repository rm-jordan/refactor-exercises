# JS 04 — Conditional explosion

Same as TypeScript exercise 04, in plain JavaScript.

**Focus:** Replace if/else chain with a lookup object.

```bash
npx vitest run exercises-js/04-conditional-explosion
npm run reset:js -- 04-conditional-explosion
```
