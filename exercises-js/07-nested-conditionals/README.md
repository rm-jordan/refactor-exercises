# JS 07 — Nested conditionals

Same as TypeScript exercise 07, in plain JavaScript.

**Focus:** Guard clauses (flatten nested ifs).

```bash
npx vitest run exercises-js/07-nested-conditionals
npm run reset:js -- 07-nested-conditionals
```
