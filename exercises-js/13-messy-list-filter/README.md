# JS 13 — Messy list filter

Same refactor as TypeScript: types via JSDoc optional, extract filters, name default max price.

```bash
npx vitest run exercises-js/13-messy-list-filter
npm run reset:js -- 13-messy-list-filter
```
