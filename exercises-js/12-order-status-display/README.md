# JS 12 — Order status display

Same refactor as TypeScript: lookup table for status → badge config.

```bash
npx vitest run exercises-js/12-order-status-display
npm run reset:js -- 12-order-status-display
```
