# 21 — Plan label (extra rep)

Same move as **04** / **12**: replace plan `if/else` chain with a lookup object.

Refactor `getPlanLabel` in `plan-label.ts`. Keep export and return strings. Tests must pass.
