# Exercise 7 — Nested Conditionals

Refactor `processPayment` in `process-payment.ts`.

**Focus:** Flatten nesting with guard clauses or a single clear condition.

**Constraints:** Keep exporting `processPayment`. All tests must pass.
