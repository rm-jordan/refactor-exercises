# 13 — Messy list filter

Refactor `getCatalogProducts` in `catalog-filter.ts`.

**Focus:** Types for products/filters, name the default max price, extract filter helpers, simplify sorting (e.g. `Array.sort`).

**Constraints:** Keep export and behavior (same filter rules, name sort ascending). Tests must pass.

**Interview note:** Common “prepare data for the table” code — ugly loops hiding simple filter + sort logic.
