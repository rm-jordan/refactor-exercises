# Exercise 4 — Conditional Explosion

Refactor `getShippingCost` in `shipping-cost.ts`.

**Focus:** Replace long if/else chain with a clearer structure (lookup table, map, etc.).

**Constraints:** Keep exporting `getShippingCost`. All tests must pass.
