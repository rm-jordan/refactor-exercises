# Exercise 8 — Data Clumps

Refactor `createShippingLabel` and `validateAddress` in `address.ts`.

**Focus:** Group related address fields (object, type, or small module).

**Constraints:** Keep both exports. All tests must pass.
