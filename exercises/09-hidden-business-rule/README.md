# Exercise 9 — Hidden Business Rule

Refactor `calculateDiscount` in `calculate-discount.ts`.

**Focus:** Make the loyalty rule (`5` years, `0.15` rate) explicit and easy to find.

**Constraints:** Keep exporting `calculateDiscount`. All tests must pass.
