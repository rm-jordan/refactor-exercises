# Exercise 1 — Types and Readability

Refactor `getActiveUsers` in `active-users.ts`.

**Focus:** Replace `any`, improve naming, use clearer iteration.

**Constraints:** Keep exporting `getActiveUsers`. All tests in `active-users.test.ts` must pass.
