# 10 — Capstone (unfamiliar code simulation)

**Do this last.** Combines several smells in one function — closer to a live screen than single-pattern exercises.

Smells present (find them yourself): `any`, plan if-chain, magic numbers, mixed concerns in one function.

**Constraints:** Keep exporting `getQuote`. All tests must pass.

If you already refactored this file, reset: `npm run reset -- 10-capstone-quote`
