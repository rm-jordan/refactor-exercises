# 19 — Login validation (extra rep)

Same move as **16** / **14**: call existing validators from the submit handler.

Refactor `submitLogin` in `login-form.ts`. Keep exports and error messages. Tests must pass.
