# Exercise 2 — Long Function

Refactor `processOrder` in `process-order.ts`.

**Focus:** Extract smaller functions, separate calculation from logging/side effects.

**Constraints:** Keep exporting `processOrder` with the same behavior. All tests in `process-order.test.ts` must pass.
