# Exercise 3 — Duplicate Logic

Refactor `getAdminDisplayName` and `getCustomerDisplayName` in `display-names.ts`.

**Focus:** Remove duplication without changing behavior.

**Constraints:** Keep both exports. All tests in `display-names.test.ts` must pass.
