# Exercise 5 — Magic Numbers

Refactor `canAccessPremiumContent` and `applySeniorDiscount` in `premium-access.ts`.

**Focus:** Name thresholds; reduce magic numbers `18` and `65`.

**Constraints:** Keep both exports. All tests must pass.
