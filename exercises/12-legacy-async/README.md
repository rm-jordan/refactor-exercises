# 12 — Legacy async

Refactor `fetchUserProfile` in `legacy-client.ts`.

**Focus:** `async/await` instead of `.then`; extract URL/header/error parsing helpers if helpful.

**Constraints:** Keep export and behavior (same URL, headers, error messages). Tests must pass.
