# Reference (unsolved)

Frozen copies of each exercise **before refactoring**. Use these to compare or reset your work in `exercises/`.

Refactor only the files under `exercises/`—not these.

Exercise 6 includes `database.ts`, `email-service.ts`, and `analytics.ts` stubs so the original `registerUser` behavior can run and be tested; those were not in the interview snippet but mirror the implied dependencies.
