# 31-long-function

**Long function** — combine types, magic number, extract pure math. ~25 min.

Suggested order: type `Order` → name `VIP_DISCOUNT_RATE` → extract `calculateOrderTotal` → thin `processOrder` (validate + log only).

Mirrors `exercises/02-long-function`.
